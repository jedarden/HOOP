{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "https://hoop.dev/schemas/dictated_note.json",
  "title": "DictatedNote",
  "description": "Metadata for a dictated note stitch — audio + transcript stored as a first-class project entity",
  "type": "object",
  "required": ["stitch_id", "recorded_at", "transcribed_at", "audio_filename", "transcript", "schema_version"],
  "properties": {
    "stitch_id": {
      "type": "string",
      "format": "uuid",
      "description": "FK to the parent stitch (kind=dictated)"
    },
    "recorded_at": {
      "type": "string",
      "format": "date-time",
      "description": "When the audio was captured"
    },
    "transcribed_at": {
      "type": "string",
      "format": "date-time",
      "description": "When Whisper transcription completed"
    },
    "audio_filename": {
      "type": "string",
      "description": "Filename of the audio attachment in ~/.hoop/attachments/<stitch_id>/"
    },
    "transcript": {
      "type": "string",
      "description": "Full text transcript of the audio"
    },
    "transcript_words": {
      "type": "array",
      "description": "Word-level timestamps from Whisper for transcript sync",
      "items": {
        "type": "object",
        "properties": {
          "word": {
            "type": "string",
            "description": "The word text"
          },
          "start": {
            "type": "number",
            "description": "Start time in seconds"
          },
          "end": {
            "type": "number",
            "description": "End time in seconds"
          }
        },
        "required": ["word", "start", "end"]
      }
    },
    "duration_secs": {
      "type": ["number", "null"],
      "description": "Audio duration in seconds"
    },
    "language": {
      "type": ["string", "null"],
      "description": "Detected language code (e.g. en, de)"
    },
    "tags": {
      "type": "array",
      "items": {
        "type": "string"
      },
      "description": "Optional tags for categorization"
    },
    "transcription_status": {
      "type": "string",
      "enum": ["Pending", "Completed", "Failed"],
      "description": "Whether transcription is pending, completed, or failed"
    },
    "redacted_words": {
      "type": "array",
      "description": "Words that have been redacted from the transcript (with timestamps for audio muting)",
      "items": {
        "type": "object",
        "properties": {
          "word_index": {
            "type": "number",
            "description": "Index into the original transcript_words array"
          },
          "original_word": {
            "type": "string",
            "description": "The original word that was redacted (for audit trail)"
          },
          "start": {
            "type": "number",
            "description": "Start timestamp of the word (for audio muting)"
          },
          "end": {
            "type": "number",
            "description": "End timestamp of the word (for audio muting)"
          },
          "redacted_at": {
            "type": "string",
            "format": "date-time",
            "description": "When the redaction was performed"
          }
        },
        "required": ["word_index", "original_word", "start", "end", "redacted_at"]
      }
    },
    "schema_version": {
      "type": "string",
      "pattern": "^\\d+\\.\\d+\\.\\d+$"
    }
  },
  "schema_version": "1.0.0"
}
