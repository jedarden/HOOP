{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "https://hoop.dev/schemas/audit_row.json",
  "title": "AuditRow",
  "description": "Audit log entry for every write HOOP performs",
  "type": "object",
  "required": ["id", "ts", "actor", "kind", "target", "result", "hash_prev", "hash_self"],
  "properties": {
    "id": {
      "type": "string",
      "format": "uuid",
      "description": "Unique audit entry ID"
    },
    "ts": {
      "type": "string",
      "format": "date-time",
      "description": "Timestamp of the action"
    },
    "actor": {
      "type": "string",
      "description": "Actor identity (e.g., 'user:jedarden', 'hoop:agent:session-id')"
    },
    "kind": {
      "type": "string",
      "enum": ["bead_created", "stitch_created", "config_changed", "project_added", "project_removed", "schema_migrated", "backup_started", "backup_finished", "backup_failed", "restore_started", "restore_finished", "restore_failed"],
      "description": "Action kind"
    },
    "target": {
      "type": "string",
      "description": "Target identifier (bead ID, stitch ID, etc.)"
    },
    "args": {
      "type": "object",
      "description": "Arguments passed to the action"
    },
    "result": {
      "type": "string",
      "enum": ["success", "failure", "partial"],
      "description": "Action result"
    },
    "error": {
      "type": ["string", "null"],
      "description": "Error message if result is not success"
    },
    "hash_prev": {
      "type": "string",
      "pattern": "^[0-9a-f]{64}$",
      "description": "SHA-256 hash of the previous audit row (64 hex chars, genesis row is all zeros)"
    },
    "hash_self": {
      "type": "string",
      "pattern": "^[0-9a-f]{64}$",
      "description": "SHA-256 hash of this audit row's content (64 hex chars)"
    },
    "schema_version": {
      "type": "string",
      "pattern": "^\\d+\\.\\d+\\.\\d+$",
      "description": "Schema version at time of action"
    }
  },
  "schema_version": "1.0.0"
}
