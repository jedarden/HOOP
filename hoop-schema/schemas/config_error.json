{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "https://hoop.dev/schemas/config_error.json",
  "title": "ConfigError",
  "description": "Configuration parse error for projects.yaml hot-reload",
  "type": "object",
  "required": ["message", "line", "col"],
  "properties": {
    "message": {
      "type": "string",
      "description": "Human-readable error message"
    },
    "line": {
      "type": "integer",
      "description": "Line number where the error occurred (1-indexed)"
    },
    "col": {
      "type": "integer",
      "description": "Column number where the error occurred (1-indexed)"
    }
  },
  "schema_version": "1.0.0"
}
