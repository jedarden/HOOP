# Bead bf-44l39: Unused utoipa::ToSchema import already removed

## Finding

The `utoipa::ToSchema` import from `hoop-daemon/src/api_uploads.rs` was already removed in commit `16086f9`:
```
refactor: remove unused utoipa::ToSchema from request-only structs
```

## Evidence

- `git show 16086f9 --stat` shows `hoop-daemon/src/api_uploads.rs | 1 -` confirming the file was modified
- Current `api_uploads.rs` has no `utoipa::ToSchema` import (only uses `#[utoipa::path(...)]` macro at line 75)
- `cargo check --package hoop-daemon` passes without errors
- The file compiles without syntax errors

## Acceptance criteria status

- ✅ Unused utoipa::ToSchema import removed from api_uploads.rs (already done in 16086f9)
- ✅ File compiles without syntax errors (verified with cargo check)
