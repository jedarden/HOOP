# bf-2e233 — Fix 54 OpenAPI ToSchema compilation errors in hoop-daemon

## Outcome

**Already complete — no code changes required.** This bead was dispatched to fix
54 `E0277` ("the trait bound `T: utoipa::ToSchema` is not satisfied") errors, but
the work had already been completed and verified by prior beads in the same
workspace (commits `bf-4q2xd`, `bf-1v7st`, `bf-3g946`, `bf-18mvt`, `bf-2oynm`,
plus the earlier `utoipa` import-cleanup beads `bf-45ygx`, `bf-4am98`, etc.).

## Verification performed (2026-07-09)

1. **`cargo check -p hoop-daemon` exits 0** — only warnings, zero errors.
   The `openapi` feature is part of `hoop-daemon`'s default feature set
   (`default = ["openapi"]` in `hoop-daemon/Cargo.toml`), so the feature-gated
   `#[cfg_attr(feature = "openapi", derive(utoipa::ToSchema))]` derives are
   active under a plain `cargo check`.

2. **E0277 error count: 0.** `cargo check` output grepped for `E0277` yields
   zero matches. All 54 errors are resolved.

3. **Every struct named in the task already derives `ToSchema`:**

   | Struct | File | Derive form |
   |--------|------|-------------|
   | `SwitchRequest` | `api_agent.rs:127` | `utoipa::ToSchema` |
   | `TurnAttachment` | `api_agent.rs:186` | `utoipa::ToSchema` |
   | `TurnRequest` | `api_agent.rs:194` | `utoipa::ToSchema` |
   | `SiblingProject` | `cross_project_propagation.rs:23` | `utoipa::ToSchema` |
   | `ApproveProposalRequest` | `api_reflection_ledger.rs:43` | `cfg_attr(feature="openapi", derive(utoipa::ToSchema))` |
   | `RejectProposalRequest` | `api_reflection_ledger.rs:61` | `cfg_attr(feature="openapi", derive(utoipa::ToSchema))` |
   | `ScriptRunRequest` | `api_scripts.rs:163` | `cfg_attr(feature="openapi", derive(utoipa::ToSchema))` |
   | `EnableTourRequest` | `api_tour_project.rs:35` | `cfg_attr(feature="openapi", derive(utoipa::ToSchema))` |
   | `ListJobsQuery` | `api_transcription.rs:20` | `utoipa::ToSchema` |
   | `CreateScreenCaptureRequest` | `api_screen_capture.rs:35` | `cfg_attr(feature="openapi", derive(utoipa::ToSchema))` |
   | `StartStreamingUploadRequest` | `api_screen_capture.rs:354` | `cfg_attr(feature="openapi", derive(utoipa::ToSchema))` |
   | `CompleteStreamingUploadRequest` | `api_screen_capture.rs:472` | `cfg_attr(feature="openapi", derive(utoipa::ToSchema))` |

4. **ToSchema-specific clippy is clean.** The only `utoipa`/`openapi`-tagged
   clippy item is `function openapi_router is never used` (`lib.rs:1277`) — a
   pre-existing dead-code warning, not a ToSchema issue.

## Out of scope

`cargo clippy -p hoop-daemon -- -D warnings` reports ~92 warnings, but they are
all **pre-existing and unrelated to ToSchema**:
- unused import `json` in `prompt_substitute.rs`
- private-interface warning in `reflection_detector.rs` (`PatternCategory`)
- dead code in `lib.rs` (`openapi_router`, `load_hoop_config`,
  `check_and_emit_capacity_alert`), `capacity.rs`, `sessions.rs`
- unused constants in `stitch_percentile_index.rs`

Cleaning those up touches unrelated modules and is out of scope for a bead whose
sole acceptance criterion is resolving the 54 `E0277` ToSchema errors — which
are resolved.
