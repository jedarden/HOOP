# bf-5460k — status --json recon (child 3 of 3 acceptance checks)

Split from bf-2dpkl. Measures the THIRD of the three bf-5mpcl acceptance checks:
`hoop status --json` returns valid JSON.

## Build

Release binary built successfully:

```
nix-shell -p pkg-config openssl --run 'cargo build --release'   # exit 0
```

Binary: `./target/release/hoop` (~43 MB, built 2026-07-26).

## Command run

```
./target/release/hoop status --json
```

## Result

- **JSON validity: YES** — `jq empty` parses stdout without error (exit 0);
  `jq type` → `"object"`, single top-level key `projects`.
- **Exit code: 0**
- **stderr:** empty (no parse error)

## Output shape

Well-formed JSON object. Top-level `projects` array enumerates the discovered
workspaces:

testrepo, FABRIC, HOOP, SIGIL, ai-code-battle, web, bead-forge, drawrace,
miroir, mobile-gaming, pdftract, spaxel, NEEDLE.

Each project carries:
- `name`
- optional `label`
- `workspaces[]` → `path`, `role` ("primary"), `beads_summary` {total, open, claimed, closed}
- aggregate counts: `total_beads`, `open_beads`, `claimed_beads`, `closed_beads`

All counts are 0 (fresh/empty bead stores in this environment), but the
structure is consistent and parseable.

## Verdict

`hoop status --json` is **healthy** — valid JSON, exit 0, no stderr. This
acceptance check (one of three in bf-5mpcl) **passes**.

Out of scope: cargo test (child 1), clippy (child 2), fixing any issues.

## Re-verification (2026-07-26, session bf-5460k dispatch)

Re-ran the full check to confirm the bead's recorded result still holds:

```
nix-shell -p pkg-config openssl --run 'cargo build --release'   # exit 0 (up to date)
./target/release/hoop status --json > out.json 2> err.txt      # EXIT=0
cat out.json | jq .                                            # JQ_EXIT=0
```

- stderr (`err.txt`) empty; stdout 5224 bytes.
- JSON validity: **YES**, exit code **0** — unchanged from the original run.

Result re-confirmed and recorded as comment 16 on the bead.
